// Helper to detect mobile viewports
function isMobile() {
  return window.matchMedia('(max-width: 768px)').matches;
}

// Global initialization when DOM is ready
document.addEventListener('DOMContentLoaded', () => {
  // Mobile touch tweaks if needed
});
